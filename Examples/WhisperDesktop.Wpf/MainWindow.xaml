<Window x:Class="WhisperDesktop.Modern.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:WhisperDesktop.Modern.Models"
        mc:Ignorable="d"
        Title="Whisper Desktop" Width="1180" Height="760" MinWidth="960" MinHeight="640"
        WindowStartupLocation="CenterScreen" Closing="OnWindowClosing">
  <Window.Resources>
    <Style x:Key="Card" TargetType="Border">
      <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
      <Setter Property="CornerRadius" Value="12" />
      <Setter Property="Padding" Value="20" />
      <Setter Property="Margin" Value="0,0,0,16" />
    </Style>
    <DataTemplate DataType="{x:Type models:TranscriptionJob}">
      <Grid Margin="0,5">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="2*" />
          <ColumnDefinition Width="120" />
          <ColumnDefinition Width="180" />
        </Grid.ColumnDefinitions>
        <StackPanel>
          <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
          <TextBlock Text="{Binding InputPath}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" TextTrimming="CharacterEllipsis" />
        </StackPanel>
        <TextBlock Grid.Column="1" Text="{Binding StatusText}" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />
        <ProgressBar Grid.Column="2" Height="7" VerticalAlignment="Center" Minimum="0" Maximum="1" Value="{Binding Progress}" />
      </Grid>
    </DataTemplate>
  </Window.Resources>

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Grid Margin="0,0,0,20">
      <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
      <StackPanel>
        <TextBlock Text="Whisper Desktop" FontSize="28" FontWeight="Bold" />
        <TextBlock Text="本地、离线的批量语音转文字工作台" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
      </StackPanel>
      <Border Grid.Column="1" Background="#E8EEFF" CornerRadius="14" Padding="12,7" VerticalAlignment="Center">
        <TextBlock Text="{Binding ModelStatus}" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" />
      </Border>
    </Grid>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="360" />
        <ColumnDefinition Width="16" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
          <Border Style="{StaticResource Card}">
            <StackPanel>
              <TextBlock Text="模型" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,12" />
              <TextBlock Text="推理引擎" Foreground="{StaticResource TextSecondaryBrush}" />
              <ComboBox ItemsSource="{Binding Engines}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedEngine}"
                        IsEnabled="{Binding CanEditQueue}" Margin="0,5,0,12" />
              <TextBox Text="{Binding ModelPath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Margin="0,0,0,10" />
              <StackPanel Orientation="Horizontal">
                <Button Content="选择模型" Click="ChooseModel_Click" />
                <Button Content="加载模型" Style="{StaticResource PrimaryButton}" Click="LoadModel_Click" IsEnabled="{Binding CanLoadModel}" />
              </StackPanel>
              <ProgressBar Height="5" Margin="0,12,0,0" Minimum="0" Maximum="1" Value="{Binding ModelProgress}" />
            </StackPanel>
          </Border>

          <Border Style="{StaticResource Card}">
            <StackPanel>
              <TextBlock Text="转录设置" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,12" />
              <TextBlock Text="语言" Foreground="{StaticResource TextSecondaryBrush}" />
              <ComboBox ItemsSource="{Binding Languages}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedLanguage}" Margin="0,5,0,12" />
              <TextBlock Text="输出格式" Foreground="{StaticResource TextSecondaryBrush}" />
              <ComboBox ItemsSource="{Binding OutputFormats}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedOutputFormat}" Margin="0,5,0,12" />
              <CheckBox Content="翻译为英文" IsChecked="{Binding Translate}" Margin="0,0,0,12" />
              <TextBlock Text="输出目录" Foreground="{StaticResource TextSecondaryBrush}" />
              <TextBox Text="{Binding OutputFolder}" IsReadOnly="True" Margin="0,5,0,8" />
              <Button Content="选择目录" Click="ChooseOutputFolder_Click" HorizontalAlignment="Left" />
            </StackPanel>
          </Border>
        </StackPanel>
      </ScrollViewer>

      <Grid Grid.Column="2">
        <Grid.RowDefinitions><RowDefinition Height="3*" /><RowDefinition Height="16" /><RowDefinition Height="2*" /></Grid.RowDefinitions>
        <Border Style="{StaticResource Card}" Margin="0">
          <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
            <Grid Margin="0,0,0,12">
              <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <TextBlock Text="任务队列" FontSize="17" FontWeight="SemiBold" />
              <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="添加文件" Click="AddFiles_Click" IsEnabled="{Binding CanEditQueue}" />
                <Button Content="移除" Click="RemoveFiles_Click" IsEnabled="{Binding CanEditQueue}" />
                <Button Content="清空" Click="ClearFiles_Click" IsEnabled="{Binding CanEditQueue}" Margin="0" />
              </StackPanel>
            </Grid>
            <ListBox x:Name="JobList" Grid.Row="1" ItemsSource="{Binding Jobs}" BorderThickness="0" Background="Transparent" SelectionMode="Extended" />
          </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0">
          <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
            <TextBlock Text="实时输出" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,10" />
            <TextBox Grid.Row="1" Text="{Binding LiveOutput}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" FontFamily="Consolas, Microsoft YaHei UI" FontSize="12" />
          </Grid>
        </Border>
      </Grid>
    </Grid>

    <Grid Grid.Row="2" Margin="0,8,0,0">
      <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
      <TextBlock Text="{Binding GlobalStatus}" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />
      <StackPanel Grid.Column="1" Orientation="Horizontal">
        <Button Content="停止" Click="Stop_Click" IsEnabled="{Binding IsRunning}" />
        <Button Content="开始转录" Style="{StaticResource PrimaryButton}" Click="Start_Click" IsEnabled="{Binding CanStart}" Margin="0" />
      </StackPanel>
    </Grid>
  </Grid>
</Window>
