<Window x:Class="WhisperDesktop.Modern.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Whisper Desktop" Width="1380" Height="860" MinWidth="1120" MinHeight="700"
        WindowStartupLocation="CenterScreen" Closing="OnWindowClosing">
  <Window.Resources>
    <Style x:Key="Card" TargetType="Border">
      <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="12" />
      <Setter Property="Padding" Value="18" />
    </Style>
    <Style x:Key="SectionTitle" TargetType="TextBlock">
      <Setter Property="FontSize" Value="17" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
    <Style x:Key="MutedText" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
    </Style>
    <Style x:Key="NavTab" TargetType="TabItem">
      <Setter Property="Width" Value="200" />
      <Setter Property="Foreground" Value="#CDD6E7" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Margin" Value="10,4" />
      <Setter Property="Padding" Value="16,13" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabItem">
            <Border x:Name="TabBorder" Background="Transparent" CornerRadius="8" Padding="{TemplateBinding Padding}">
              <ContentPresenter ContentSource="Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="TabBorder" Property="Background" Value="#243455" />
                <Setter Property="Foreground" Value="White" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="TabBorder" Property="Background" Value="#20304F" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="220" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Border Background="#17233B">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Margin="24,28,20,24">
          <TextBlock Text="WHISPER" Foreground="White" FontSize="22" FontWeight="Bold" />
          <TextBlock Text="D E S K T O P" Foreground="#7F91B4" FontSize="11" FontWeight="SemiBold" />
        </StackPanel>
        <Border Grid.Row="2" Margin="16,0,16,20" Background="#202F4D" CornerRadius="8" Padding="12">
          <StackPanel>
            <TextBlock Text="当前模型" Foreground="#8798B8" FontSize="11" />
            <TextBlock Text="{Binding ModelStatus}" Foreground="White" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" />
          </StackPanel>
        </Border>
      </Grid>
    </Border>

    <TabControl Grid.ColumnSpan="2" TabStripPlacement="Left" Background="Transparent" BorderThickness="0" Padding="0">
      <TabControl.Resources>
        <Style TargetType="TabItem" BasedOn="{StaticResource NavTab}" />
      </TabControl.Resources>

      <TabItem Header="批量字幕" Margin="0,82,0,-73">
        <Grid Background="{StaticResource WindowBackgroundBrush}" Margin="0">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>

          <Grid Margin="28,24,28,18">
            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel>
              <TextBlock Text="批量字幕" FontSize="26" FontWeight="Bold" Foreground="#FF4F6CFF" />
              <TextBlock Text="按文件或目录导入媒体，勾选后批量生成字幕" Style="{StaticResource MutedText}" Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
              <Button Content="添加文件" Click="AddFiles_Click" IsEnabled="{Binding CanEditQueue}" />
              <Button Content="添加文件夹" Click="AddFolder_Click" IsEnabled="{Binding CanEditQueue}" Style="{StaticResource PrimaryButton}" Margin="0" />
            </StackPanel>
          </Grid>

          <Grid Grid.Row="1" Margin="28,0,28,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="230" />
              <ColumnDefinition Width="14" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource Card}">
              <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                <StackPanel Margin="0,0,0,12">
                  <TextBlock Text="来源目录" Style="{StaticResource SectionTitle}" />
                  <TextBlock Text="取消勾选可排除整个目录" Style="{StaticResource MutedText}" FontSize="11" Margin="0,3,0,0" />
                </StackPanel>
                <ListBox Grid.Row="1" ItemsSource="{Binding SourceFolders}" BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Stretch">
                  <ListBox.ItemTemplate>
                    <DataTemplate>
                      <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,9">
                        <Grid>
                          <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                          <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Top" Margin="0,2,8,0" />
                          <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Path}" Style="{StaticResource MutedText}" FontSize="10" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding FileCount, StringFormat={}{0} 个文件}" Foreground="{StaticResource PrimaryBrush}" FontSize="10" Margin="0,3,0,0" />
                          </StackPanel>
                        </Grid>
                      </Border>
                    </DataTemplate>
                  </ListBox.ItemTemplate>
                </ListBox>
              </Grid>
            </Border>

            <Grid Grid.Column="2">
              <Grid.RowDefinitions><RowDefinition Height="3*" /><RowDefinition Height="14" /><RowDefinition Height="2*" /></Grid.RowDefinitions>
              <Border Style="{StaticResource Card}">
                <Grid>
                  <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                  <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                      <TextBlock Text="任务队列" Style="{StaticResource SectionTitle}" />
                      <TextBlock Text="{Binding QueueSummary}" Style="{StaticResource MutedText}" Margin="12,3,0,0" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                      <Button Content="全选" Click="SelectAll_Click" IsEnabled="{Binding CanEditQueue}" Style="{StaticResource TextButton}" />
                      <Button Content="全不选" Click="SelectNone_Click" IsEnabled="{Binding CanEditQueue}" Style="{StaticResource TextButton}" />
                      <Button Content="移除" Click="RemoveFiles_Click" IsEnabled="{Binding CanEditQueue}" Style="{StaticResource TextButton}" />
                      <Button Content="清空" Click="ClearFiles_Click" IsEnabled="{Binding CanEditQueue}" Style="{StaticResource TextButton}" Margin="0" />
                    </StackPanel>
                  </Grid>
                  <DataGrid x:Name="JobGrid" Grid.Row="1" ItemsSource="{Binding Jobs}" AutoGenerateColumns="False"
                            SelectionMode="Extended" SelectionUnit="FullRow" CanUserAddRows="False" IsReadOnly="False">
                    <DataGrid.Columns>
                      <DataGridCheckBoxColumn Header="处理" Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="58" />
                      <DataGridTextColumn Header="文件名" Binding="{Binding FileName}" Width="2*" IsReadOnly="True" />
                      <DataGridTextColumn Header="相对路径" Binding="{Binding RelativePath}" Width="2*" IsReadOnly="True" />
                      <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="90" IsReadOnly="True" />
                      <DataGridTemplateColumn Header="进度" Width="130" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                          <DataTemplate><ProgressBar Minimum="0" Maximum="1" Value="{Binding Progress}" Height="6" /></DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                      </DataGridTemplateColumn>
                    </DataGrid.Columns>
                  </DataGrid>
                </Grid>
              </Border>

              <Border Grid.Row="2" Style="{StaticResource Card}">
                <TabControl Style="{StaticResource ContentTabs}" Background="#FF959595">
                  <TabItem Header="字幕预览">
                    <DataGrid ItemsSource="{Binding PreviewSegments}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,10,0,0">
                      <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding Index}" Width="45" />
                        <DataGridTextColumn Header="文件" Binding="{Binding FileName}" Width="150" />
                        <DataGridTextColumn Header="时间戳" Binding="{Binding RangeText}" Width="190" />
                        <DataGridTextColumn Header="字幕内容" Binding="{Binding Text}" Width="*" />
                      </DataGrid.Columns>
                    </DataGrid>
                  </TabItem>
                  <TabItem Header="运行日志">
                    <TextBox Text="{Binding LogOutput}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" FontFamily="Consolas, Microsoft YaHei UI" FontSize="11" Margin="0,10,0,0" Background="#FFCACACA" />
                  </TabItem>
                </TabControl>
              </Border>
            </Grid>
          </Grid>

          <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,18,0,0" Padding="28,14">
            <Grid>
              <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
              <StackPanel>
                <TextBlock Text="{Binding GlobalStatus}" FontWeight="SemiBold" />
                <TextBlock Text="请先在“模型与设置”中加载模型" Style="{StaticResource MutedText}" FontSize="11" Margin="0,2,0,0" />
              </StackPanel>
              <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="停止" Click="Stop_Click" IsEnabled="{Binding IsRunning}" />
                <Button Content="开始转录" Click="Start_Click" IsEnabled="{Binding CanStart}" Style="{StaticResource PrimaryButton}" Margin="0" Padding="24,10" />
              </StackPanel>
            </Grid>
          </Border>
        </Grid>
      </TabItem>

      <TabItem Header="实时字幕" Margin="1,89,-1,-82">
        <ScrollViewer Background="{StaticResource WindowBackgroundBrush}" VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="28,24" MaxWidth="920" HorizontalAlignment="Left">
            <TextBlock Text="实时字幕" FontSize="26" FontWeight="Bold" Foreground="#FF4F6CFF" />
            <TextBlock Text="从麦克风采集语音，逐句显示带时间戳的字幕" Style="{StaticResource MutedText}" Margin="0,4,0,20" />
            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
              <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel>
                  <TextBlock Text="录音设备" Style="{StaticResource SectionTitle}" />
                  <ComboBox ItemsSource="{Binding CaptureDevices}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCaptureDevice}" Margin="0,12,0,6" />
                  <TextBlock Text="{Binding LiveStatus}" Style="{StaticResource MutedText}" FontSize="11" />
                </StackPanel>
                <Button Grid.Column="1" Content="刷新设备" Click="RefreshDevices_Click" VerticalAlignment="Bottom" Margin="14,0,0,0" />
              </Grid>
            </Border>
            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
              <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="14" /><ColumnDefinition /></Grid.ColumnDefinitions>
                <StackPanel>
                  <TextBlock Text="源语言" Style="{StaticResource MutedText}" />
                  <ComboBox ItemsSource="{Binding Languages}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedLanguage}" Margin="0,6,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="2">
                  <TextBlock Text="输出格式" Style="{StaticResource MutedText}" />
                  <ComboBox ItemsSource="{Binding OutputFormats}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedOutputFormat}" Margin="0,6,0,0" />
                </StackPanel>
              </Grid>
            </Border>
            <Border Background="#FFF8E7" BorderBrush="#F2D58A" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,0,14">
              <StackPanel>
                <TextBlock Text="实时流式转录正在接入" FontWeight="SemiBold" Foreground="#6F5314" />
                <TextBlock Text="设备发现和界面流程已经完成；下一阶段会把麦克风 PCM 分块送入 CUDA 后端，并补上暂停、断句和保存。" TextWrapping="Wrap" Foreground="#806722" Margin="0,4,0,0" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}">
              <Grid Height="260">
                <TextBlock Text="开始录音后，带时间戳的实时字幕将显示在这里" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Grid>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="模型与设置" Margin="-3,97,3,-89">
        <ScrollViewer Background="{StaticResource WindowBackgroundBrush}" VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="28,24" MaxWidth="920" HorizontalAlignment="Left">
            <TextBlock Text="模型与设置" FontSize="26" FontWeight="Bold" Foreground="#FF4F6CFF" />
            <TextBlock Text="管理推理引擎、模型、语言和字幕输出策略" Style="{StaticResource MutedText}" Margin="0,4,0,20" />

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
              <StackPanel>
                <TextBlock Text="推理模型" Style="{StaticResource SectionTitle}" Margin="0,0,0,14" />
                <TextBlock Text="推理引擎" Style="{StaticResource MutedText}" />
                <ComboBox ItemsSource="{Binding Engines}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedEngine}" IsEnabled="{Binding CanEditQueue}" Margin="0,6,0,14" />
                <TextBlock Text="GGML 模型文件" Style="{StaticResource MutedText}" />
                <Grid Margin="0,6,0,10">
                  <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                  <TextBox Text="{Binding ModelPath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" />
                  <Button Grid.Column="1" Content="选择模型" Click="ChooseModel_Click" Margin="10,0,0,0" />
                </Grid>
                <Grid>
                  <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                  <ProgressBar Height="7" Minimum="0" Maximum="1" Value="{Binding ModelProgress}" VerticalAlignment="Center" />
                  <Button Grid.Column="1" Content="加载模型" Click="LoadModel_Click" IsEnabled="{Binding CanLoadModel}" Style="{StaticResource PrimaryButton}" Margin="16,0,0,0" />
                </Grid>
              </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0,0,0,14">
              <StackPanel>
                <TextBlock Text="识别与翻译" Style="{StaticResource SectionTitle}" Margin="0,0,0,14" />
                <TextBlock Text="源语言" Style="{StaticResource MutedText}" />
                <ComboBox ItemsSource="{Binding Languages}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedLanguage}" Margin="0,6,0,12" />
                <CheckBox Content="使用 Whisper 翻译为英文" IsChecked="{Binding Translate}" IsEnabled="{Binding CanTranslate}" />
                <TextBlock Text="Whisper 原生翻译任务只输出英文；中文翻译将在后续接入独立翻译引擎。" Style="{StaticResource MutedText}" FontSize="11" Margin="24,5,0,0" />
              </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
              <StackPanel>
                <TextBlock Text="字幕输出" Style="{StaticResource SectionTitle}" Margin="0,0,0,14" />
                <TextBlock Text="输出格式" Style="{StaticResource MutedText}" />
                <ComboBox ItemsSource="{Binding OutputFormats}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedOutputFormat}" Margin="0,6,0,12" />
                <TextBlock Text="输出位置" Style="{StaticResource MutedText}" />
                <ComboBox ItemsSource="{Binding OutputLocations}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedOutputLocation}" Margin="0,6,0,4" />
                <TextBlock Text="{Binding SelectedOutputLocation.Description}" Style="{StaticResource MutedText}" FontSize="11" Margin="0,0,0,12" />
                <TextBlock Text="输出目录" Style="{StaticResource MutedText}" />
                <Grid Margin="0,6,0,0">
                  <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                  <TextBox Text="{Binding OutputFolder}" IsReadOnly="True" />
                  <Button Grid.Column="1" Content="选择目录" Click="ChooseOutputFolder_Click" Margin="10,0,0,0" />
                </Grid>
              </StackPanel>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </TabItem>
    </TabControl>
  </Grid>
</Window>
